package renderfarm.loadbalancer;

/**
 * Represents the weight of the request.
 * NOTE: These levels are different from the instance load level because
 * it would be possible have different scales in both.
 * @author Andre
 *
 */
public enum RequestWeight {
	HEAVY //TODO make the real levels
}
